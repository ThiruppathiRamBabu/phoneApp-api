const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "root",
    password: "AmIndia12",
    database: "sys",
  },
  listPerPage: 10,
};
module.exports = config;